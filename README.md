## Setup

```bash
ln -s ~/.dots/tmux/.tmux.conf ~/.tmux.conf
ln -s ~/.dots/tmuxinator/.tmuxinator ~/.tmuxinator
ln -s ~/.dots/vim/.vimrc ~/.vimrc
ln -s ~/.dots/zsh/.zprofile ~/.zprofile
ln -s ~/.dots/zsh/.zshrc ~/.zshrc
```
